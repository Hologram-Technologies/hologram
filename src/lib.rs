//! Hologram Greenfield — O(1) compute acceleration via pre-computed lookup tables.
//!
//! This crate re-exports the public API from all workspace crates so consumers
//! only need to depend on `hologram-greenfield`.

pub use holo_archive;
pub use holo_core;
pub use holo_exec;
pub use holo_graph;
