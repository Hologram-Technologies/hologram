//! CUDA compute backend (NVIDIA GPU).
//!
//! Low priority. Implements `CudaMemory` and `CudaBackend` for device-native
//! execution on NVIDIA GPUs. Requires cuDNN/cuBLAS for optimized kernels.

// TODO: CudaMemory + CudaBackend implementation (low priority).
