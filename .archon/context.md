## Ecosystem Context (auto-generated by archon)

You are working in **hologram** — O(1) compute acceleration runtime built on UOR-Framework

### Your dependencies

| Repo | Role | Key types & traits |
|------|------|--------------------|
| UOR-Framework | core | `DetectedEnum`, `GenerationReport`, `NamespaceMapping`, `RustFile`, `all_namespace_iris()`, +15 more; `ConformanceReport`, `Severity`, `TestResult`, `WorkspacePaths`, `run_all()`, +20 more; `OntologyIndex`, `check_claims()`, `escape_html()`, `expand_directives()`, `fragment_from_iri()`, +8 more; `AchievabilityStatus`, `ComplexityClass`, `CoordinateKind`, `FiberState`, `GeometricCharacter`, +213 more; `AnnotationProperty`, `Class`, `Individual`, `IndividualValue`, `Namespace`, +26 more; `BreadcrumbItem`, `NamespaceSummary`, `NavItem`, `Page`, `SearchEntry`, +19 more |

### Who depends on you

- **hologram-ai** — AI inference library providing ONNX, GGUF, and GGML model support for Hologram
- **hologram-network** — Distributed computation and storage using iroh p2p networking
- **hologram-sandbox** — Provides isolated execution environments (process sandboxes, WASM runtimes, microVMs) for secure, resource-controlled task execution
- **hologram-vm** — Interactive VM runtime for managing long-lived WASM sandbox sessions with wire protocol support