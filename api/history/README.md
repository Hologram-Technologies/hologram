Per-release public-API snapshots, archived at release by `version-bump.yml`
(`api/history/v<version>/<crate>.txt`). Each release diffs against the previous
archive to produce the `api/CHANGELOG.md` entry.
