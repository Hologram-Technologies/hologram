[package]
name = "hologram-bare-hal"
version = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
authors = { workspace = true }
description = "Bare-metal hardware-abstraction traits (bare-metal spec §3.2.1): BlockDevice + NetworkInterface — the substrate-internal seam between device-agnostic store/net backends and per-device drivers. no_std + alloc."

[dependencies]
async-trait = { workspace = true }
spin        = { workspace = true }

[dev-dependencies]
pollster = { workspace = true }

[features]
default = []
std = []
